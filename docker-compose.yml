version: "2"
services:
  mongo:
    image: mongo
    restart: always
    ports:
    - "27017:27017"
    volumes:
    - ./data/db:/data/db
    command: "--syslog"
  backend:
    container_name: chordlib-back
    restart: always
    build: ./
    ports:
    - "3000:3000"
    links:
    - mongo
    volumes:
    - ./public:/usr/www/public
